fn main() {
    // Define variables
    let x = 5.0;
    let y = 3.0;

    // Perform calculations
    let result = x + y;

    // Print results
    println!("The sum of {} and {} is {}", x, y, result);
}
